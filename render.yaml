# render.yaml - Render 部署配置

services:
  - type: web
    name: ea-fc-live-dashboard
    env: docker
    region: oregon
    buildCommand: docker build -t ea-fc-live .
    startCommand: docker run -p 8000:8000 ea-fc-live
    healthCheckPath: /
    healthCheckMethod: GET
