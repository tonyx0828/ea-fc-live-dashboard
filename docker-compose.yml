version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_FOOTBALL_KEY=${API_FOOTBALL_KEY:-demo_key}
      - DEBUG=true
    volumes:
      - ./data:/app/data
    restart: unless-stopped
